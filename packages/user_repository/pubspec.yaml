name: user_repository
description: A new Flutter package project.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  equatable: ^2.0.5
  rxdart: ^0.27.7  # ← AFEGEIX AQUESTA LÍNIA

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true